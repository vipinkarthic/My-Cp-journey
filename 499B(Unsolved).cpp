#include <bits/stdc++.h>

using namespace std;
// char check(char x){
//     for int(y = 0; y < m; y++){
//         if(pair_array[i].first == x){
//             if(pair_array[i].first>pair_array[i].second){
//                 return 
//             }
//         }
//     }
//} 

int main() {
    int n,m;
    string a,b,c;
    cin >> m;
    pair<string,string> pair_array[m];
    for(int i =0;i<m; i++){
        pair<string,string> p1;
        cin >> p1.first >> p1.second ;
        pair_array[i] = p1;
    }
    string array[m];
    for(int i = 0; i<m ; i++){
        cin >> c;
        array [i] = c;
    }
    
    for (int k= 0; k< m ; k++){

            
        }
    }
